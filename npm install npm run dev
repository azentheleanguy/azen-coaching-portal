Then open `http://localhost:3000`. Note: the check-in form and dashboards won't
save data locally unless you also set up Vercel KV environment variables in a
`.env.local` file — for just previewing the design, that's fine to skip.
